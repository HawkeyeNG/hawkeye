{
  "name": "hawkeye-ui-checks",
  "private": true,
  "description": "CI guards for the Hawkeye front end: layout budgets, asset budgets, link reachability, contrast and visual regression.",
  "scripts": {
    "test": "playwright test budget.spec.js layout.spec.js",
    "audit": "playwright test audit.spec.js --reporter=list",
    "update-snapshots": "playwright test layout.spec.js --update-snapshots"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.1",
    "@playwright/test": "1.50.1"
  }
}
