# SonarQube Cloud automatic analysis scope. Test harnesses and build/ops scripts
# run only on our own machines; left in, they buried the shipped-code findings
# (225 of 412 "vulnerabilities" were localhost test servers).
sonar.exclusions=tests/**,tmp/**,scripts/**,native/scripts/**,mobile/scripts/**
