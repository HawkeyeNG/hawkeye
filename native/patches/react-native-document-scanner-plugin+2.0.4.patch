diff --git a/node_modules/react-native-document-scanner-plugin/android/src/main/java/com/documentscanner/DocumentScannerModule.kt b/node_modules/react-native-document-scanner-plugin/android/src/main/java/com/documentscanner/DocumentScannerModule.kt
index fbb6836..eb25da4 100644
--- a/node_modules/react-native-document-scanner-plugin/android/src/main/java/com/documentscanner/DocumentScannerModule.kt
+++ b/node_modules/react-native-document-scanner-plugin/android/src/main/java/com/documentscanner/DocumentScannerModule.kt
@@ -52,6 +52,12 @@ class DocumentScannerModule(reactContext: ReactApplicationContext) :
     val documentScannerOptionsBuilder = GmsDocumentScannerOptions.Builder()
       .setResultFormats(GmsDocumentScannerOptions.RESULT_FORMAT_JPEG)
       .setScannerMode(GmsDocumentScannerOptions.SCANNER_MODE_FULL)
+      // HAWKEYE PATCH (patch-package): live capture ONLY. The stock module never
+      // calls this, and the Play-services scanner UI then offers gallery import —
+      // which lets any saved photo masquerade as a freshly captured EC8A sheet.
+      // Hawkeye stamps GPS/time at capture, so an imported image would pass
+      // freshness and location checks while showing a sheet from anywhere.
+      .setGalleryImportAllowed(false)
 
     if (options.hasKey("maxNumDocuments")) {
       documentScannerOptionsBuilder.setPageLimit(
diff --git a/node_modules/react-native-document-scanner-plugin/ios/DocScanner/DocScanner.swift b/node_modules/react-native-document-scanner-plugin/ios/DocScanner/DocScanner.swift
index 251c633..3e19c97 100644
--- a/node_modules/react-native-document-scanner-plugin/ios/DocScanner/DocScanner.swift
+++ b/node_modules/react-native-document-scanner-plugin/ios/DocScanner/DocScanner.swift
@@ -26,6 +26,17 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
     /** @property  croppedImageQuality the 0 - 100 quality of the cropped image */
     private var croppedImageQuality: Int
 
+    /**
+     HAWKEYE PATCH (patch-package): how many pages to KEEP.
+
+     VisionKit has no page limit — VNDocumentCameraViewController exposes no
+     such property, so the scanner will always invite another scan and there
+     is no supported way to stop it. This caps what is written to disk and
+     returned, which is what the Android side already does through
+     setPageLimit(). nil keeps every page, matching the stock behaviour.
+     */
+    private var maxNumDocuments: Int?
+    
     /**
      constructor for DocScanner
 
@@ -44,7 +55,8 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
         errorHandler: @escaping (String) -> Void = {_ in },
         cancelHandler: @escaping () -> Void = {},
         responseType: String = ResponseType.imageFilePath,
-        croppedImageQuality: Int = 100
+        croppedImageQuality: Int = 100,
+        maxNumDocuments: Int? = nil
     ) {
         self.viewController = viewController
         self.successHandler = successHandler
@@ -52,6 +64,7 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
         self.cancelHandler = cancelHandler
         self.responseType = responseType
         self.croppedImageQuality = croppedImageQuality
+        self.maxNumDocuments = maxNumDocuments
     }
     
     /**
@@ -97,7 +110,8 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
         errorHandler: @escaping (String) -> Void = {_ in },
         cancelHandler: @escaping () -> Void = {},
         responseType: String? = ResponseType.imageFilePath,
-        croppedImageQuality: Int? = 100
+        croppedImageQuality: Int? = 100,
+        maxNumDocuments: Int? = nil
     ) {
         self.viewController = viewController
         self.successHandler = successHandler
@@ -105,6 +119,7 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
         self.cancelHandler = cancelHandler
         self.responseType = responseType ?? ResponseType.imageFilePath
         self.croppedImageQuality = croppedImageQuality ?? 100
+        self.maxNumDocuments = maxNumDocuments
         
         self.startScan()
     }
@@ -122,8 +137,18 @@ public class DocScanner: NSObject, VNDocumentCameraViewControllerDelegate {
     ) {
         var results: [String] = []
 
-        // loop through all scanned pages
-        for pageNumber in 0...scan.pageCount - 1 {
+        // HAWKEYE PATCH: keep at most maxNumDocuments pages. An EC8A is one
+        // sheet; anything after the first is a page the observer did not mean
+        // to submit, and writing it out only leaves a stray file behind.
+        let pageLimit = min(scan.pageCount, self.maxNumDocuments ?? scan.pageCount)
+        guard pageLimit > 0 else {
+            goBackToPreviousView(controller)
+            self.errorHandler("No pages were scanned")
+            return
+        }
+
+        // loop through the scanned pages we are keeping
+        for pageNumber in 0..<pageLimit {
             
             // convert scan UIImage to jpeg data
             guard let scannedDocumentImage: Data = scan
diff --git a/node_modules/react-native-document-scanner-plugin/ios/DocumentScanner.swift b/node_modules/react-native-document-scanner-plugin/ios/DocumentScanner.swift
index 49d49e2..5aba242 100644
--- a/node_modules/react-native-document-scanner-plugin/ios/DocumentScanner.swift
+++ b/node_modules/react-native-document-scanner-plugin/ios/DocumentScanner.swift
@@ -40,7 +40,10 @@ public class RNDocumentScanner: NSObject {
                     self.documentScanner = nil
                 },
                 responseType: options["responseType"] as? String,
-                croppedImageQuality: options["croppedImageQuality"] as? Int
+                croppedImageQuality: options["croppedImageQuality"] as? Int,
+                // HAWKEYE PATCH: the stock bridge drops this on the floor, so
+                // maxNumDocuments was Android-only and iOS returned every page.
+                maxNumDocuments: options["maxNumDocuments"] as? Int
             )
         }
     }
